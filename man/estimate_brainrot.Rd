% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_brainrot.R
\name{estimate_brainrot}
\alias{estimate_brainrot}
\title{Estimate Weighted Means Using Brainrot Data}
\usage{
estimate_brainrot(mockdata, variable, by = NULL)
}
\arguments{
\item{mockdata}{A \code{data.frame} representing the loaded mock data (e.g. \code{MOCKDATA.csv}).}

\item{variable}{A string. The name of the numeric column to estimate (e.g. "monthly_grind_income").}

\item{by}{Optional string. The name of a grouping variable to compute subgroup-wise means (e.g. "state_of_decay").}
}
\value{
A survey-weighted mean estimate (with SEs). If \code{by} is specified, returns a data frame of estimates by group.
}
\description{
This function computes weighted means of any numeric variable using the
\code{survey} package, based on an external \code{MOCKDATA.csv} file
Users must load the data themselves using \code{read.csv()} or equivalent.
}
\examples{
\dontrun{
# Load external mock data install from website: atharvdeshpande.com
mockdata <- read.csv("path-to/MOCKDATA.csv")

# Estimate overall mean
estimate_brainrot(mockdata, variable = "monthly_grind_income")

# Estimate by state
estimate_brainrot(mockdata, variable = "monthly_grind_income", by = "state_of_decay")
}

}
